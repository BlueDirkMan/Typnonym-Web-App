<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>PROFILE PAGE</h1>
    <h2><%= searchedUser.username %> </h2>
    <h2><%= searchedUser.email %> </h2>
    <h2><%= searchedUser.bio %> </h2>
    <a href="/user/<%= searchedUser._id %>/edit">edit</a>
    <a href="/user/<%= searchedUser._id %>/delete">delete</a>
    <a href="/user/<%= searchedUser._id %>/score">score</a>
    <h1>Scores</h1>
    <% for (let score of searchedScore) { %>
        <p><%= score.points %> </p>    
        <p><%= score.date %> </p>    
        <p><%= score.owner %> </p>    
    <% } %>  
</body>
</html>